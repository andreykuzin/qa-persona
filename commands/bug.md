---
description: Promote a row from bugs.md to a GitHub issue via gh CLI; back-link the issue number into the bug tracker.
argument-hint: <bug ID, e.g. "3">
---

Invoke the `qa-persona-bug` skill via the Skill tool with `args: $ARGUMENTS`. The skill body contains the duplicate-promotion guard, gh-CLI invocation, and back-link rules.
