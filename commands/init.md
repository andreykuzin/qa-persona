---
description: Bootstrap qa-persona on this codebase — runs Phases 1–4 (study → personas → catalog → Layer 1 scaffold) with stop-and-confirm gates.
argument-hint: [optional flow name, e.g. "marketplace"]
---

Invoke the `qa-persona-init` skill via the Skill tool with `args: $ARGUMENTS`. The skill body contains the full workflow including the stop-and-confirm gates — do not proceed without invoking it.
