---
description: Add, remove, or edit a persona — keeps personas.md and seed-personas.sh in lockstep, refuses to silently drop emails referenced by scenarios.
argument-hint: <add|remove|edit> <natural-language description>
---

Invoke the `qa-persona-persona` skill via the Skill tool with `args: $ARGUMENTS`. The skill body contains the CRUD logic, reference-scan safeguard, and lockstep-edit rules.
