---
description: Drive Layer 2 (browser walkthrough) for one or more scenarios — capture screenshots, fill in the Run #N narrative. Optional --parallel mode for E-category concurrency.
argument-hint: <scenario IDs, e.g. "A1 D1" or "all"> [--parallel]
---

Invoke the `qa-persona-walkthrough` skill via the Skill tool with `args: $ARGUMENTS`. The skill body contains the sequential workflow, the `--parallel` sub-agent dispatch protocol, and the persona-isolation contract.
